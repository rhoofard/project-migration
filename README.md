This program takes an existing project, repo, issues, and labels and copies them over to another org.

Looks for a file called "config.yml" to pull in info from

Assumes you have a token with access to both orgs with permissions to create projects, issues, and repos (if flag is selected)
